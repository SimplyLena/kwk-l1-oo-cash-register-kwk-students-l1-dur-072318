# Code your cash register here!
class AmazonTransaction
  def initialize(total=0,items=[],discount=20, amazon_transaction.total = 100)
    @total = total
    @items = items
    @discount = discount
    @current_total = current_total
  end 
end
